#pragma once
#include "stdafx.h"
using namespace std;

//*///------------------------------------------------------------------------------------------
//*///------------------------------------------------------------------------------------------
struct InitParams
{
	//Tipes of parametrs.
	enum ParamNames 
	{ 
		RTPPort, outerPort, innerPort, MediaPath, outerIP, innerIP, MaxTimeAnn, MaxTimeCnf, MaxTimePrx,
		LogLevel, PathEXE,
		MaxParamNames
	};
	//Methods for Data.
	InitParams(char*);
	string GetParams();
	void GetPathExe(char*);
	void ParseConfigFile();


	//Data.
	vector<string> data;
	string error = "";
};
typedef shared_ptr<InitParams> SHP_InitParams;
//*///------------------------------------------------------------------------------------------
//*///------------------------------------------------------------------------------------------
class Socket
{
public:
	Socket(string, int, boost::asio::io_service&);
	boost::asio::ip::udp::socket s;
private:
	
};
typedef shared_ptr<Socket> SHP_Socket;
//*///------------------------------------------------------------------------------------------
//*///------------------------------------------------------------------------------------------
struct OuterMes
{
	~OuterMes();
	boost::asio::ip::udp::endpoint sender;
	char* rawMes = new char[2048];
	//char rawMes[2048];
};
//*///------------------------------------------------------------------------------------------
//*///------------------------------------------------------------------------------------------
//*///------------------------------------------------------------------------------------------
//*///------------------------------------------------------------------------------------------
//*///------------------------------------------------------------------------------------------
//*///------------------------------------------------------------------------------------------

